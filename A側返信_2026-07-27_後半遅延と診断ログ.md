# A側返信（後半遅延の切り分けと診断ログ）— 担当A（昇）→ 担当B（角谷さん）

作成: 2026-07-27
反映コミット: `a98e609`（getStats診断ログ）
関連: `A側返信_2026-07-27_パッチ完了.md`

---

## 0. 背景

前回の通話試験で **後半に遅延が出た** 件、原因の切り分けが必要という話になりました。
「時間とともに悪化する」症状なので、**何かが蓄積している**サインと捉え、A側で調査＋計測手段を用意しました。

---

## 1. まずA側コードのリーク監査 → リークなし

蓄積しうる箇所（音声ソース／sink要素／iceキュー／タイマー／イベントリスナー）を全点検。
いずれも**上限あり・解放ありで境界内**でした。

実測（1対1・60秒）でも次のとおり**すべて一定**：

| 指標 | 5秒 → 60秒 |
|---|---|
| peers | 1 → 1 |
| 音声ソース | 2 → 2 |
| sink要素 | 1 → 1 |
| JSヒープ | 2〜3MB（増加なし） |
| RTT / 損失 / フレーム落ち | 1ms / 0% / 0（安定） |

→ **A側connection.jsが後半遅延の原因である可能性は低い**と判断します。
ただしB側UI・録画や端末・回線までは断定できないため、下記の計測手段を用意しました。

---

## 2. 診断ログを実装（`a98e609`・既定OFF・契約②に無影響）

`getStats()` を定期採取し、経時変化を記録します。

**使い方（どちらでも）:**
- `test.html?...&diag=1` を付ける → 5秒ごとにコンソール出力
- コード中で `cm.startDiagnostics({ intervalMs: 5000 })` / `cm.stopDiagnostics()` / `cm.downloadDiagnostics()`（JSON保存）

**採る指標:** RTT・送信RTT・ジッタ・損失率・受信/送信kbps・復号fps・累積フレーム落ち・
経路(host/srflx/relay)、および自前リソース数(peers/音声ソース/sink/iceキュー/JSヒープ)。

**読み方（経時で悪化するものを見る）:**

| 経時で悪化 | 原因 |
|---|---|
| RTT・ジッタ・損失率 ↑ | **回線**（帯域不足・輻輳・NAT） |
| 復号fps ↓・フレーム落ち ↑（RTT安定） | **端末**（CPU/熱・mesh人数過多） |
| 送信kbpsが上がり続け頭打ち→RTT↑ | **上り帯域の飽和**（mesh人数×ビットレート） |
| 自前リソース数・ヒープ ↑ | **自前コードのリーク**（A側は一定を確認済み） |

---

## 3. 現時点の有力仮説（コードでも回線でもない可能性）

後半遅延の候補として、次の2つを特に疑っています。

1. **上り帯域の飽和** — meshは「人数−1本」を同時送信。通話が進むとWebRTCが送信ビットレートを
   上げるため、人数が多いと上りが飽和して遅延が育つ。**パッチ④の帯域制御はモバイルのみで、
   PCには上限が無い**のが気になる点です。
2. **ノートPCの熱** — 複数映像のエンコードで発熱→サーマルスロットリングで後半だけ重くなる。

どちらも診断ログの「送信kbpsが上がり続ける」「復号fpsが落ちる」で判別できます。

---

## 4. お願い・相談

1. **後半遅延を再現した通話で `diag=1` を付けて記録**していただけますか。
   `cm.downloadDiagnostics()` のJSONを共有いただければ、上表で当たりを付けます。
   （何人での通話だったか・PCかスマホかも添えていただけると助かります）
2. その結果、**上り飽和**が確認されたら、**PC側にも送信ビットレート上限**を入れます（費用ゼロ）。
   画質とのトレードオフなので、**データを見てから**が順序として正しいと考えています。
3. B側UI・録画側でも、経時で増えるDOM要素やタイマーが無いか、併せてご確認いただけると切り分けが早いです。

---

## 5. 状態

| 項目 | 状態 |
|---|---|
| A側リーク監査 | ✅ リークなし（実測60秒で一定） |
| 診断ログ | ✅ 実装・検証済み（`a98e609`） |
| 後半遅延の原因 | 🔍 データ待ち（上り飽和／端末熱を疑い中） |
| PC側ビットレート上限 | ⏸ データ確認後に判断 |
