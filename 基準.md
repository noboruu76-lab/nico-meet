# NICO Meet（自前ZOOM） — 基準.md

最終更新: 2026-07-28
状態: 開発中（担当A=昇 / 担当B=角谷さん の2人分業）

---

## 概要

相手にインストールさせず、ブラウザだけで動く自前ビデオ会議。少人数は完全P2P（WebRTC mesh）でメディアがサーバーを通らないため、サーバー費はほぼ0。

※このフォルダは独立gitリポジトリ。ホーム側リポジトリへはpushしない。

---

## フォルダ構成（2026-07-28 確定）

| 場所 | 内容 | 担当 |
|---|---|---|
| `server/signaling.py` | シグナリング中継＋`web/`静的配信＋`/health`・`/ice-config` | A |
| `server/Dockerfile` `requirements.txt` | サーバー実行環境 | A |
| `server/turn/turnserver.conf` `docker-compose.yml` | coturn（TURN）設定 | A |
| `server/README.md` | サーバー・TURN・デプロイ手順 | A |
| `web/connection.js` | 契約②実装 `ConnectionManager`（本物） | A |
| `web/config.js` | `getIceServers()`（STUN/TURN取得・フォールバック） | A |
| `web/test.html` | A用 接続確認ページ（`fakemedia`/`diag`/`vbitrate`対応） | A |
| `web/test-contract.html` | 契約②適合検査（全16項目PASS） | A |
| `web/index.html` `app.js` `ui.js` `style.css` `recorder.js` `mock-connection.js` | UI・録画・モック（**未作成**） | B |
| `minutes/` | 録画→議事録（**未着手**） | B |
| `README.md` `contract.md` `結合手順.md` `railway.toml` | 入口・契約・引継ぎ・デプロイ設定 | A |
| `A側回答_*.md` / `A側返信_*.md` | 担当A→B へのやり取り | A |

---

## 重要ファイル

| ファイル | 役割 |
|---|---|
| `基準.md` | このプロジェクトの正本 |
| `README.md` | 入口・起動手順 |
| `結合手順.md` | 担当B向け：使えるAPI・実装例・切り分け表（まずこれ） |
| `contract.md` | 契約①②（変更は2人合意） |
| `server/README.md` | サーバー・TURN・デプロイ手順（担当A） |
| `railway.toml` | デプロイ設定 |

---

## 禁止事項

- 重要ファイルの削除は必ず確認する
- `.env` やAPIキー・TURN認証情報を共有しない
- `contract.md` の契約①②は2人合意なしに変更しない
- ホーム側リポジトリへpushしない（独立repo）

---

## 現フェーズ

WebRTC mesh結合・後半遅延の切り分け中。診断ログ＋PC送信ビットレート上限（opt-in）まで実装・検証済み。
**原因特定はB側の `diag=1` 実測データ待ち**（上り飽和／端末熱を疑い中）。

---

## 次のアクション

- [x] 結合手順.md の切り分け表を最新化する（2026-07-28・§6.6/6.7）
- [x] フォルダ構成を確定して追記する（2026-07-28）
- [x] PC送信ビットレート上限を opt-in で実装・検証（2026-07-28・既定OFF、`setMaxVideoBitrate()`/`&vbitrate=`）
- [ ] Bから `diag=1` の実測JSONを受領 → 上り飽和か端末熱かを判定
- [ ] 上り飽和が確認されたらPC上限値を決めて有効化（費用ゼロ）

---

## 起動

```bash
cd server
pip install -r requirements.txt
uvicorn signaling:app --reload --port 8000
```

---

## 作業ログタグ

`~/.claude/作業ログ/` で `[NICO Meet]` を検索
