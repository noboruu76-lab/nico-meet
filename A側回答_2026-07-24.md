# A側からの回答 — 担当A（昇）→ 担当B（角谷さん）

作成: 2026-07-24
対象: 角谷さんの「結合検証の報告と差分メモ」への回答
反映コミット: `6b65182`（フォールバック＋/health）, `1de6750`（実カメラ検証）, `5553c1e`（シグナリング防御）

---

## 0. まず御礼と結論

詳細な検証と差分の洗い出し、ありがとうございます。**指摘はすべて的確**でした。
特に 2-4 の不正JSON・2-2 のフォールバックは、こちらの見落としです。**全て修正・検証済み**です。

S1〜S9 完了と公開URL稼働、お疲れさまです。`test-contract.html` 16項目PASSのご確認もありがとうございます。

---

## 1. 確認事項（§5）への回答

### 1. `connection.js` のデバイス無しフォールバック — ✅ A側で対応しました

ご指摘のとおり実装しました。段階的に降格します。

| 状況 | `local-stream` で届くstream |
|---|---|
| 正常 | 映像1 + 音声1 |
| カメラ無し | 音声1のみ |
| マイク無し | 映像1のみ |
| 両方無し/拒否 | **トラック0本**（視聴専用） |

**`addTransceiver(..., {direction:'recvonly'})` のご助言もそのとおりでした。**
これが無いとSDPにm-lineが立たず、相手の映像を永久に受け取れません。音声・映像とも実装済みです。

検証: トラック0本の参加者が相手映像を `framesDecoded=120` まで復号、`connectionState=connected`。
トラック0本でも `toggleMic/toggleCam/getMixedAudioTrack()` は例外を投げません。

### 2. ヘルスチェック用の口 — ✅ 追加しました

```
GET /health
→ {"status":"ok","rooms":0,"clients":0,"turn_configured":false}
```

`railway.toml` に `healthcheckPath = "/health"` も設定済みです。

> **警告の件、ありがとうございます。** こちらの実装は当初から**数のみ**で、部屋名・参加者名は返していません。
> 念のためテストで「部屋名 `SECRET-ROOM` / 参加者名 `SECRET-USER` が応答に含まれないこと」を検証し、
> コードとREADMEに「数だけ返す」方針を明記しました。

### 3. `?fakemedia=0` での実カメラ確認 — ✅ 済ませました

**`?fakemedia=0` で実デバイスになるというご理解で正しいです。**

実機で確認しました。

| 項目 | 結果 |
|---|---|
| カメラ | HP True Vision HD Camera 640x480@30fps |
| 2者間P2P | `connectionState = connected` |
| 送信 | 2,069,002 bytes / 160フレーム |
| 受信 | 2,086,475 bytes / 159フレーム |

**取り違え防止のため、`test-contract.html` の上部にメディアモードのバナーを追加しました。**
疑似メディアで走った結果か実デバイスの結果か、ページを見れば分かります。

なお `test.html` は逆に**既定が実デバイス**（`&fakemedia=1` で疑似）です。用途が違うためですが、
紛らわしいので `結合手順.md` §6.5 に明記しました。

### 4. TURN（coturn）の実VPS — 当面は立てない方針です

判断理由:

| | STUNのみ（現状） | TURN追加 |
|---|---|---|
| 費用 | 0円 | VPS 月500〜1000円程度 |
| 繋がる範囲 | 家庭内・一般的な回線は概ねOK | 企業網・対称NATでも繋がる |

開発と社内デモはSTUNで足りると判断しました。**社外で繋がらない事例が出た時点で立てます。**
設定ファイル（`server/turn/`）は準備済みなので、VPSさえ用意できれば30分程度で立ち上がります。

---

## 2. 差分（§2）への対応

### 2-1. `config.js` のAPI名 — 吸収いただきありがとうございます

B側で吸収済みとのこと、助かります。A側は `getIceServers(httpBaseUrl)` / `/ice-config` のまま維持します。

### 2-2. デバイス無しフォールバック — 対応済み（上記1参照）

### 2-3. 疑似メディアが既定ON — バナー表示を追加（上記3参照）

### 2-4. `signaling.py` の差分 — **2件を修正しました**

#### ① 不正JSONで接続が切れる → 修正しました

**ご指摘のとおりの不具合でした。** `json.loads` の例外は `except WebSocketDisconnect` に捕まらず、
`finally` に落ちて接続が破棄されていました。壊れたJSON・非dictは**その1通を捨てて継続**します。

#### ② 中継typeのホワイトリスト → 導入しました

**ご指摘いただいて調べたところ、想定より深刻でした。**
任意のtypeを中継していたため、**偽装した `leave` を他人へ送って会議から追い出すことが可能**でした。

`offer` / `answer` / `ice` のみ転送し、それ以外は破棄します。
検証で、契約外type `evil` と偽装 `leave` が相手に届かないことを確認しました。

#### ③ 静的配信のキャッシュ → `Cache-Control: no-store` を追加しました

開発中に古いJS/HTMLが出る問題、こちらでも同様に対処しました。

#### ④ 同名ユーザー再入室

「A側の方が親切」との評価ありがとうございます。現状の**古い接続を切って上書き**する方式を維持します。

### 2-5. TURNの時限クレデンシャル

A側方式（`use-auth-secret` / HMAC-SHA1）に寄せていただけるとのこと、賛成です。
静的パスワードをクライアントに配ると、TURN中継を第三者に使われて帯域を持っていかれるため、
時限方式の方が安全です。実装は `server/signaling.py` の `_turn_credentials()` をご参照ください。

---

## 3. こちらから相談したいこと

### ⚠️ 視聴専用の参加者が、他の参加者から見えません

フォールバック実装の結果として判明しました。

**送信トラックが0本の参加者は、他の参加者に `peer-joined` が発火しません。**
`peer-joined` が「相手の映像/音声が届いた時」に発火する仕様のためです。

- 影響: カメラもマイクも無い人が入室しても、**参加者一覧に出てこない**
- 参加者一覧（ロスター）を出すには**契約②の変更が必要**なため、独断では変更していません

対応案（いずれも契約②の変更＝2人合意が必要）:

| 案 | 内容 | 影響 |
|---|---|---|
| A | `peer-joined` を接続確立時にも発火（空streamで） | 既存UIが空streamを受け取る想定になる |
| B | ロスター用の新イベントを追加 | 契約②にイベントが1つ増える |
| C | 現状維持 | 視聴専用者は一覧に出ない |

**UIを持つB側のご意見を伺いたいです。** 必要なら実装します。

### 💡 実機で得た知見：マイク占有は珍しくありません

検証中、こちらのPCでマイクが `NotReadableError`（Intel Smart Sound Technology の既知不具合）になり、
**フォールバックが実際に作動しました**。映像のみで通話は成立しています。

UI側へのお願いとして、`stream.getAudioTracks().length === 0` を見て
**「音声なしで参加中」を表示**できると親切かと思います。ご検討ください。

### ❓ 公開URLへの接続確認について

**角谷さんの公開URL（`nico-meet-production.up.railway.app`）に、A側の `connection.js` を繋いで
動作確認してもよいでしょうか。** 契約①が一致しているので繋がるはずですが、
そちらの環境に負荷をかけるため、許可をいただいてからにします。

---

## 4. A側の状況

| # | 内容 | 状態 |
|---|---|---|
| A-M1 | シグナリング＋2者mesh | ✅ |
| A-M2 | 4人mesh・音声mix・トグル | ✅ |
| A-M3 | TURN設定・Railwayデプロイ構成 | ✅（実デプロイは未実施） |
| A-M4 | SFU自動切替 | 将来（5人以上が必要になったら） |

切り替え条件の1・2は揃いました。**公開URLが稼働中なので当面は現状維持で問題ありません。**
切り替えのタイミングは角谷さんのご都合に合わせます。
