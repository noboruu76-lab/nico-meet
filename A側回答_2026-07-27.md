# A側からの回答（第2回）— 担当A（昇）→ 担当B（角谷さん）

作成: 2026-07-27
対象: 角谷さんの「NICO Meet 引き継ぎ資料（2026-07-27）」への回答
反映コミット: この文書と同じPRに含む（connection.js / signaling.py / config.js）

---

## 0. 結論

引き継ぎ資料、精度が高くて助かりました。**§5の12項目をA側の実コードと照合**し、
A側に欠けていた**再接続(#4)・Chrome録音対策(#7)・無音トラック(#2)を実装・検証済み**です。
費用ゼロで効く**STUN多重化(§3③)**も入れました。

**TURN用VPSは当面立てない方針**です（判断済み）。設定は準備済みなので、必要になれば動かせます。

横浜↔大阪の成功、そして新幹線ケースの原因特定（TURN不在）、ありがとうございます。原因分析は妥当です。

---

## 1. §5 移植リスト12項目 — A側の実状（コード照合済み）

角谷さんのcloneは7/24時点とのことなので、**今のA側に何があるか**を実コードで確認しました。

### `web/connection.js`

| # | 項目 | A側の状況 | 対応 |
|---|---|---|---|
| 1 | デバイス無しフォールバック4段 | ✅ 元からある | — |
| 2 | 無音トラックでの視聴専用 | ❌ 空ストリームだった → **今回実装** | ✅ 対応 |
| 3 | offer側のみ `addTransceiver(recvonly)` | ⚠️ recvonlyは有るが両側付与 | 現状維持（下記） |
| 4 | WS自動再接続・送信キュー | ❌ 無かった → **今回実装** | ✅ 対応 |
| 5 | `resumeIfDropped()` | ❌ 無かった → **今回実装** | ✅ 対応 |
| 6 | AudioMixerの安定トラック | ✅ 元からある | — |
| 7 | AudioMixerの`<audio>`経由 | ❌ 無かった → **今回実装** | ✅ 対応 |

### `server/signaling.py`

| # | 項目 | A側の状況 |
|---|---|---|
| 8 | `/health`（数のみ） | ✅ ある |
| 9 | `Cache-Control: no-store` | ✅ ある |
| 10 | 同名再入室の引き継ぎ | ✅ ある（元から） |
| 11 | 転送typeホワイトリスト | ✅ ある（前回対応済み） |
| 12 | `ADMIN_TOKEN`付き`/api/rooms` | ❌ 無し（優先度低・未対応） |

**#3について:** A側はoffer/answer両側でrecvonlyを付けています。テストした範囲では問題は出ていませんが、
angeさんの「offer側のみ」の方が堅牢という指摘は理解しています。m-line順序の事故を避けるためですね。
実害が確認されたら寄せます。判断材料として、A側で両側付与にしている理由は
「answer側も自分のデバイス構成に応じてm-lineを持ちたい」ためですが、offer側限定でも受信は成立します。

---

## 2. 今回実装したもの（検証結果つき）

### #4 WS自動再接続 — 新幹線・トンネル対策の要

角谷さんの`a2aac58`の設計に沿って実装しました。

- **WS断でpeerを畳まない**（確立済みP2Pは維持）
- **指数バックオフ＋ジッター**で再接続（上限12回）
- **iceのみキュー**（TTL10秒・上限200）。offer/answerは貯めない
- **復帰後のpeers照合**で幽霊除去
- **`resumeIfDropped()`** — `online`/`visibilitychange`で即再接続

**検証（実測）:** 通話確立中にサーバーを強制停止 →
`ws.readyState=3`（切断）でも **`peers=1`・`pc=connected`（P2P生存）**、バックオフ再接続が稼働。
サーバー再起動後、**両クライアントともWS復帰・`peers=1`維持・`pc=connected`**。

> ⚠️ **照合ロジックで1つ落とし穴を踏み、修正しました。**
> 最初の実装は「peersリストに居ない相手を落とす」でしたが、これだと**サーバー再起動時に
> peersリストが空になり、P2Pが生きている相手まで誤って切って**しまいました（テストで発見）。
> **修正:** peersに居ない かつ `pc.connectionState` が `failed`/`closed` の時だけ落とす。
> P2Pの生死を真実の情報源にしています。**角谷さんの実装がこの罠を踏んでいないかご確認ください。**

### #7 Chrome録音対策 — 相手の声が録音に入らない問題

相手streamを`<audio muted>`要素にも流し、ChromeでWebAudioへpullされるようにしました。
実際の音はUIの`<video>`が出すので`muted`です。**検証:** peerごとにsink要素が1つ生成されることを確認。

> 角谷さんの「**消さないこと**」の警告、そのまま踏襲しています。

### #2 無音トラック視聴専用 — 前回の相談事項を解決

前回私が「視聴専用者が見えない（契約②変更が要るかも）」と相談した件、
**角谷さんの無音トラック方式が答え**でした。A側もこれに合わせ、契約②変更は不要になりました。

- 送信デバイスが無い時、**無音トラック1本**（`enabled=false`）を送る
- **検証:** 送信デバイス無しの参加者が、相手側で `peer-joined` 発火＝**一覧に出る**ことを確認

**追加した公開プロパティ（UI用・additive）:** `cm.hasLocalVideo` / `cm.hasLocalAudio`（実デバイスの有無）。
無音プレースホルダを送っても「実マイクは無い」とUIが判定でき、「音声なし」表示に使えます。
既存の契約②の名前は一切変えていません。

### STUN多重化（§3③）— 費用ゼロの単一障害点対策

- `/ice-config` が**複数STUN**を返すように（`STUN_URLS` 環境変数・カンマ区切り）
- 既定で Google + Cloudflare の2系統。`config.js` のフォールバックも2系統に
- **検証:** `/health` の `stun_count=2`、`/ice-config` が2系統返すことを確認
- `turns:443` 用の `TURN_TLS_HOST` も受けるように（TURN導入時に有効）

**回帰確認:** 契約②適合検査 **全16項目PASS維持**。通常の2者mesh・正常な退出も正常。

---

## 3. §8のご質問への回答

| # | ご質問 | 回答 |
|---|---|---|
| 1 | TURN用VPSを立てられるか | **当面立てません。** 開発・社内デモはSTUN多重化で凌ぐ。社外で対称NAT失敗が出たら立てる（設定は準備済み・`turns:443`対応も入れた） |
| 2 | A側にある/ない12項目 | **§1の表のとおり。** 欠けていた#2/#4/#5/#7は今回実装。#12のみ未対応（優先度低） |
| 3 | パッチ③④どちらが持つか | **A側（私）が持ちます**（`connection.js`はA所管）。③は§4参照 |
| 4 | `peer-failed`イベント追加 | §4参照。**#2解決で当面は不要**と考えますが、③導入時に再検討 |

---

## 4. パッチ③（ICE restart適正化）について

**同意します。現状のA側は`onconnectionstatechange`ハンドラ自体が存在しない**ので、
基地局切替やICE失敗に無反応です（§3⑤はA側にも当てはまります）。A側で実装します。

ただし1点、**`peer-failed`は契約②へのイベント追加＝2人合意事項**です。提案:

- まず`disconnected`→5秒待ちの`restartIce`（無限ループ防止・回数制限つき）を**イベント追加なしで**入れる
- `peer-failed`（UIに「接続失敗」を出す）が本当に要るか、UI設計を持つ角谷さんと相談してから追加

TURNが無い今、`failed`で`restartIce`しても成功しないのは角谷さんの指摘どおりです。
なので**回数制限（3回）で打ち切る**部分は先に入れます。イベント発火の要否だけ相談させてください。

**パッチ④（モバイル帯域）もA側で持ちます。** モバイル判定で640×360/20fps、`maxBitrate`と
`degradationPreference`を設定。次のPRで入れます。

---

## 5. こちらからの確認・お願い

1. **照合ロジックの罠（§2の#4警告）** — 角谷さんの再接続実装が、サーバー再起動時に
   生きているP2Pを誤って落としていないかご確認ください。踏んでいたら修正案を共有します。
2. **公開URLへの接続確認** — 前回もお願いした件、**A側`connection.js`を公開URLに繋いで
   動作確認してよいか**、改めて許可をいただけますか（負荷をかけるため）。
3. **切り替え方針** — §5移植リストの欠落を今回埋めたので、A側実装はB側の
   ロバスト性に追いつきました。切り替えのタイミングは角谷さんのご都合に合わせます。

---

## 6. 現在のA側マイルストーン

| # | 内容 | 状態 |
|---|---|---|
| A-M1〜A-M3 | mesh・録画用mix・TURN設定・デプロイ構成 | ✅ |
| 追加対応 | フォールバック・/health・シグナリング防御・**再接続・Chrome録音・無音トラック・STUN多重化** | ✅ |
| パッチ③④ | ICE restart適正化・モバイル帯域 | 🔜 次PR（A側で実装） |
| TURN実運用 | coturn on VPS | ⬜ 保留（設定は準備済み） |
| A-M4 | SFU自動切替 | 将来（5人以上） |
