# A側返信（③④完了）— 担当A（昇）→ 担当B（角谷さん）

作成: 2026-07-27
関連: `A側回答_2026-07-27.md`（§5監査の詳細はこちら）
反映コミット: `bf68c56`（再接続/Chrome録音/無音トラック/STUN多重化）, `66fad71`（パッチ③④）

---

## 0. まとめ

精度の高い引き継ぎ資料、ありがとうございました。
**§5の12項目をA側の実コードと照合**し、欠けていた機能をすべて実装・検証しました。
前回「次PR」とした**パッチ③④も完了**しています。すべてpush済み・契約②適合16項目PASS維持です。

---

## 1. 対応した内容（すべて検証済み）

### §5でA側に欠けていた機能（`bf68c56`）

| 項目 | 内容 |
|---|---|
| WS自動再接続（#4） | WS断でpeerを畳まず、指数バックオフ+ジッターで再接続。iceのみキュー、resumeIfDropped |
| Chrome録音対策（#7） | 相手音声を `<audio>` 経由でpull。「消さないこと」の警告そのまま踏襲 |
| 無音トラック視聴専用（#2） | 空ストリームをやめ、無音トラック1本で他者から可視化 |
| STUN多重化（§3③） | Google+Cloudflareの2系統。単一障害点を解消 |

### パッチ③④（`66fad71`）

| パッチ | 内容 |
|---|---|
| ③ ICE自動復旧 | disconnected→5秒→restart、failed→3回制限→復旧不能なら `peer-left` で畳む。**`peer-failed` は追加せず契約②維持**。initiator側のみrestartでグレア回避。recoverTimerのリーク解除 |
| ④ モバイル帯域制御 | モバイルのみ 640×360/20fps・500kbps。PCは従来どおり |

**検証:** initiator側 restartIce で ice-ufrag 変化(Qzsz→BLfE)・接続維持、非initiatorは正しく no-op、
`_applyMobileBitrate` が maxBitrate/degradationPreference を例外なく反映。回帰なし。

---

## 2. ⚠️ 確認していただきたい罠（重要）

再接続の**照合ロジックで罠を踏み、修正しました。**

- 最初の実装: 「peersリストに居ない相手を落とす」
- 問題: **サーバー再起動時に peersリストが空になり、P2Pが生きている相手まで誤切断**（テストで発見）
- 修正: `pc.connectionState` が `failed`/`closed` の時だけ落とす（P2Pの生死を真実の情報源にする）

**角谷さんの再接続実装が同じ罠を踏んでいないかご確認ください。** 踏んでいたら修正案を共有します。

---

## 3. §8のご質問への回答

| # | ご質問 | 回答 |
|---|---|---|
| 1 | TURN用VPS | **当面立てません**（判断済み）。設定は準備済み・`turns:443`対応も入れた。社外で対称NAT失敗が出たら動かす |
| 2 | A側にある/ない12項目 | `A側回答_2026-07-27.md` §1の表のとおり。#12（`/api/rooms`）のみ未対応 |
| 3 | パッチ③④ | **A側で実装完了**（`66fad71`） |
| 4 | `peer-failed`イベント | #2解決と③の`peer-left`畳みで**当面不要**と考えます。UI通知が要るか、角谷さんの設計を伺ってから追加を検討 |

---

## 4. こちらからのお願い

**公開URL（`nico-meet-production.up.railway.app`）にA側の `connection.js` を繋いで動作確認してよいでしょうか。**
負荷をかけるため、許可をいただいてからにします。

A側で単独でできる実装はこれで出揃いました。切り替えのタイミングは角谷さんのご都合に合わせます。
