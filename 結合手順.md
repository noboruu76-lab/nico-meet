# 結合手順 — 担当A（昇）→ 担当B（角谷さん）引き渡し

**A側は完成・検証済みです。** Bは契約②のAPIだけを見てUIを書けば、差し替えるだけで繋がります。
（A作成 / 2026-07-24）

---

## 1. Aが用意したもの（Bはこれらを編集しない）

| ファイル | 役割 |
|---|---|
| `server/signaling.py` | シグナリング中継。`web/`の静的配信も兼ねる |
| `web/connection.js` | **本物の`ConnectionManager`**（契約②の実装）。Bはこれをimportするだけ |
| `web/config.js` | `getIceServers()` — STUN/TURN設定をサーバーから取得 |
| `web/test.html` | A用の接続確認ページ（Bは使わなくてよい） |
| `web/test-contract.html` | **契約②適合検査**（全16項目PASS済み）。実装例としても読める |

---

## 2. まず動かす

```bash
cd server
pip install -r requirements.txt
uvicorn signaling:app --reload --port 8000
```

ブラウザで確認（2タブ開くと相手が出る）:
- 契約検査: `http://localhost:8000/test-contract.html?room=c1&user=alice`
- 接続確認: `http://localhost:8000/test.html?room=r1&user=u1&fakemedia=1`

`&fakemedia=1` はカメラ無しでも動く疑似映像モード。実カメラで試すなら外す。

---

## 3. Bが作るファイル（Aは触りません）

```
web/index.html          画面本体
web/app.js              起動・?mock=1 で モック/本物 を切替
web/ui.js               映像グリッド・操作バー
web/style.css
web/recorder.js         録画（MediaRecorder＋音声mix）
web/mock-connection.js  Aを待たずUIを動かすモック（契約②準拠）
minutes/                録画→議事録（既存transcribe.py連携）
```

---

## 4. 使えるAPIはこれだけ（契約②・全て検証済み）

```js
import { ConnectionManager } from './connection.js';
import { getIceServers } from './config.js';

const iceServers = await getIceServers(location.origin);
const wsProto = location.protocol === 'https:' ? 'wss' : 'ws';

const cm = new ConnectionManager({
  room, user,
  signalingUrl: `${wsProto}://${location.host}/ws`,
  iceServers,
});

// イベント（joinの前に登録すること）
cm.on('local-stream', (stream) => { /* 自分の映像 → <video>.srcObject */ });
cm.on('peer-joined',  ({ peerId, stream }) => { /* 相手のタイルを追加 */ });
cm.on('peer-left',    ({ peerId }) => { /* タイルを削除 */ });
cm.on('mode-changed', ({ mode }) => { /* 今は 'mesh' が1回だけ来る */ });

await cm.join();          // 入室（カメラ許可ダイアログはこの中で出る）

cm.toggleMic(false);      // マイクOFF / true でON
cm.toggleCam(false);      // カメラOFF / true でON
const track = cm.getMixedAudioTrack();  // 録画用：全員の声が1本に入った音声トラック
cm.leave();               // 退出
```

### 保証していること（`test-contract.html`で全PASS確認済み）
- `local-stream` は音声・映像トラックを持つ `MediaStream`
- `peer-joined` の `peerId` は文字列、`stream` は `MediaStream`
- `getMixedAudioTrack()` は `kind='audio'` / `readyState='live'`
- **人数が増減しても、最初に受け取ったmixトラックは生き続ける**
  → 録画中に人が出入りしても音が途切れない。**録画開始時に1回取得すればOK**
- `toggleMic/toggleCam` は実トラックの `enabled` を切り替える

### デバイスが無い/拒否された場合（2026-07-24 追加）
`join()` は**失敗しません**。次の順に自動で降格します。

| 状況 | `local-stream` で届くstream |
|---|---|
| 正常 | 映像1 + 音声1 |
| カメラ無し | 音声1のみ |
| マイク無し | 映像1のみ |
| 両方無し/拒否 | **トラック0本**（視聴専用。相手の映像音声は受信できる） |

- UI側は `stream.getVideoTracks().length` / `getAudioTracks().length` で判定してください
  （0本ならタイルにアバターや名前を出す、など）
- トラック0本でも `toggleMic/toggleCam/getMixedAudioTrack()` は**例外を投げません**（安全に無視されます）
- 検証済み: トラック0本の参加者が、相手の映像を受信・復号できることを確認（`framesDecoded > 0`）

**実機での確認（2026-07-24・担当A環境）**
- 実カメラ `HP True Vision HD Camera` で 640x480@30fps を取得、2者間で双方向P2P成立
  （送信 2,069,002 bytes / 160フレーム、受信 2,086,475 bytes / 159フレーム、`connectionState=connected`）
- **同環境でマイクは `NotReadableError`（他アプリが占有）となり、フォールバックが実際に作動した**
  → 映像のみで入室・通話が成立。フォールバックが無ければ入室自体が失敗していた
- 教訓: マイク占有は珍しくない。**UIは「音声なしで参加中」を表示できるようにしておくと親切**

### ⚠️ 既知の制約：視聴専用の参加者は「相手側に見えません」
送信トラックが0本の参加者は、**他の参加者に `peer-joined` が発火しません**。
`peer-joined` は「相手の映像/音声が届いた時」に発火する仕様のため、届くものが無いと発火しないためです。

- 影響: カメラもマイクも無い人が入室しても、他の人の参加者一覧に出てきません
- **参加者一覧（ロスター）が必要なら、契約②に手を入れる必要があります**。必要になったらAへ相談してください（2人合意事項）

### 注意
- `cm._audioSources` など **`_` 始まりは内部用**。使わないでください（予告なく変わります）
- イベント登録は `join()` の**前**に。`local-stream` は `join()` 内ですぐ発火します

## サーバーの死活確認（2026-07-24 追加）

```
GET /health
→ {"status":"ok","rooms":0,"clients":0,"turn_configured":false}
```

- `clients` は今シグナリングに繋がっている総人数
- `turn_configured` が `false` なら**STUNのみ**（TURN未設定）
- 「繋がらない」時の切り分けに使ってください（このURLが応答しない＝サーバーが落ちている）

---

## 5. モック → 本物への切り替え（＝結合S4）

`app.js` をこの形にしておけば、切り替えは**URLパラメータだけ**で済みます。

```js
const params = new URLSearchParams(location.search);
const useMock = params.get('mock') === '1';

const { ConnectionManager } = useMock
  ? await import('./mock-connection.js')   // B作成のモック
  : await import('./connection.js');       // A作成の本物

// 以降のコードは共通（同じ契約②だから）
```

- 開発中: `index.html?mock=1&room=r1&user=me` … サーバー不要でUIだけ動かせる
- 結合時: `?mock=1` を外す … `uvicorn`を起動して2タブで開けば2人通話

**UIのコードは一行も変えずに繋がる想定**です。もし変更が要るなら契約のズレなので、Aに連絡してください。

---

## 6. 困ったときの切り分け

| 症状 | 見るところ |
|---|---|
| 相手が出てこない | サーバー起動しているか / 2タブの `room` が同じか / `user` が別々か |
| 自分の映像が出ない | カメラ許可を出したか。`&fakemedia=1` で切り分け |
| 本物にすると動かない | まず `test-contract.html` を2タブで開く。全PASSならA側は正常＝UI側の使い方の問題 |
| 録画に相手の声が入らない | `getMixedAudioTrack()` を録画開始時に取得しているか（`local-stream`ではなくこちら） |

`test-contract.html` が全16項目PASSする限り、A側の契約違反ではありません。

---

## 6.4 追加された堅牢化（2026-07-27 追記）

`connection.js` / `signaling.py` に以下を実装済み（契約②の名前は不変）。

| 機能 | UI側で知っておくこと |
|---|---|
| **WS自動再接続** | WS断でもタイルを消さない。復帰後も同じ`peers`が維持される。UI側で切断時にpeerを消す処理は不要 |
| **無音トラック視聴専用** | デバイス無しでも相手から見える。`cm.hasLocalVideo`/`cm.hasLocalAudio`（実デバイスの有無）で「音声なし/映像なし」表示ができる |
| **Chrome録音対策** | `getMixedAudioTrack()`に相手の声が入るよう内部で`<audio>`を使用。UI側の変更不要 |
| **STUN多重化** | `/ice-config`が複数STUNを返す。UI側の変更不要 |
| **ICE自動復旧（パッチ③）** | 基地局切替・一過性断で自動的にICE restart。復旧不能な相手は既存の`peer-left`で畳む（UIはタイルを消すだけでよい。新イベントなし） |
| **モバイル帯域制御（パッチ④）** | モバイル端末は送信を640x360/20fps・500kbpsに自動で絞る。PCは従来どおり。UI側の変更不要 |

- `cm.hasLocalVideo` / `cm.hasLocalAudio` は**追加された読み取り専用プロパティ**（既存APIは不変）。
- 再接続で「諦めた」時はコンソールにエラーを出すのみ（イベントは未追加）。UI通知が要るなら相談を。

## 6.6 診断ログ（「後半だけ遅い」の切り分け・2026-07-27 追記）

通話が時間とともに遅くなる症状の原因が「回線」「端末」「自前コード」のどれかを切り分ける。

**使い方（どちらでも）:**
- `test.html?...&diag=1` を付ける → 5秒ごとにコンソールへ出力
- コード中で `cm.startDiagnostics({ intervalMs: 5000 })` / `cm.stopDiagnostics()` / `cm.downloadDiagnostics()`

**採る指標:** RTT・送信RTT・ジッタ・損失率・受受信/送信kbps・復号fps・累積フレーム落ち・
経路(host/srflx/relay)、および自前リソース数(peers/音声ソース/sink/iceキュー/JSヒープMB)。

**読み方（経時で“増える/悪化する”ものを見る）:**

| 経時で悪化するもの | 原因の切り分け |
|---|---|
| RTT・ジッタ・損失率 ↑ | **回線**の問題（帯域不足・輻輳・NAT） |
| 復号fps ↓・累積フレーム落ち ↑（RTTは安定） | **端末**の問題（CPU/熱・mesh人数過多） |
| 送信kbpsが上がり続けて頭打ち→RTT↑ | **上り帯域の飽和**（mesh人数×ビットレート）。パッチ④の帯域制御対象 |
| 自前リソース数・JSヒープ ↑ | **自前コードのリーク**（今のところA側は一定を確認済み） |

- A側で60秒計測した実測では、peers/音声ソース/sink/JSヒープ すべて**一定**（リークなし）。
- 実際の「後半遅延」を再現した通話で `diag=1` を付けて記録し、上表で当たりを付けてください。

## 6.5 検査ページのメディアモード（2026-07-24 追記）

`test-contract.html` は**既定で疑似メディア**（カメラ/マイクを使わないダミー）で走ります。
実デバイスで検査するには `?fakemedia=0` を付けてください。
どちらで走ったかはページ上部にバナーで表示されます（結果の取り違え防止）。

- `test.html` は逆で、**既定が実デバイス**・`&fakemedia=1` で疑似になります（用途が違うため）

## 7. Aの状況（2026-07-24時点）

- **A-M1〜A-M3 完了**（2者/4者mesh・音声mix・TURN設定・Railwayデプロイ構成）
- 未完了: **実デプロイ**（Railway/coturnのVPSが必要）と **A-M4（SFU・将来）**
- 公開URL（S9）が必要になったら `server/README.md` の手順で進めます

## 8. 契約の変更について

`contract.md` の契約①②を変えたい時は**2人合意**が必要です（片方だけで変えると結合が壊れます）。
Bの実装で不都合が出たら、勝手に回避せずAへ相談してください。
